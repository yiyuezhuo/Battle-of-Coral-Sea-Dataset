# Julia package `BattleOfCoralSeaData`

Data itself is encoded by light type Julia source to make to edit. 
While we will type them when we read back them from JSON.

## make dataset

```julia
using BattleOfCoralSeaData

make()
```